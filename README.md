# Manish Portfolio

Static portfolio (plain HTML/CSS/JS) designed for GitHub Pages.

Dark mode is available via the header 'Mode' button (saves preference in localStorage and respects system preference).